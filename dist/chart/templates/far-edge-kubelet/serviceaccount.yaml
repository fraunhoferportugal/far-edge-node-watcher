apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "far-edge-kubelet.fullname" . }}
  labels:
    {{- include "far-edge-node-watcher.labels" . | nindent 4 }}
automountServiceAccountToken: true
