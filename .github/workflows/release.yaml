name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release"
        required: false
        type: string
        default: ""
      skip-app-release:
        description: "Whether to build and release the application or only its $chart_name"
        required: false
        type: boolean
        default: false

  workflow_call:
    inputs:
      tag:
        description: "Tag to release"
        required: false
        type: string
        default: ""
      skip-app-release:
        description: "Whether to build and release the application or only its $chart_name"
        required: false
        type: boolean
        default: false

  push:
    tags:
      - '**'

jobs:
  validate-tag:
    runs-on: self-hosted
    outputs:
      tag: ${{ steps.tag.outputs.tag-name }}
      semver-valid: ${{ steps.check-tag.outputs.valid }}
    steps:
      - uses: actions/checkout@v6

      - name: Determine tag
        id: tag
        run: |
          if [ -n "${{ inputs.tag }}" ]; then
            TAG_NAME="${{ inputs.tag }}"
          elif [[ "$GITHUB_REF" =~ refs/tags/(.+) ]]; then
            TAG_NAME="${BASH_REMATCH[1]}"
          else
            echo "No tag found"
            exit 1
          fi
          echo "tag-name=$TAG_NAME" >> $GITHUB_OUTPUT

      - name: Check if tag is a valid SemVer
        id: check-tag
        run: |
          python3 -u .github/scripts/semver_regex.py
        env:
          TAG: ${{ steps.tag.outputs.tag-name }}

  set-skip-app-release:
    needs: validate-tag
    if: needs.validate-tag.outputs.semver-valid == 'true'
    outputs:
      skip-app-release: ${{ steps.skip.outputs.skip-app-release }}
    runs-on: self-hosted
    steps:
      - name: Set skip app release
        id: skip
        run: |
          if [ -n "${{ inputs.skip-app-release }}" ]; then
            skip_app_release="${{ inputs.skip-app-release }}"
          else
            skip_app_release="false"
          fi
          echo "skip-app-release=$skip_app_release" >> $GITHUB_OUTPUT

  test-and-lint:
    name: Test and Lint
    needs: set-skip-app-release
    if: needs.set-skip-app-release.outputs.skip-app-release == 'false'
    uses: ./.github/workflows/test-and-lint.yaml

  build-and-publish:
    name: Build and publish
    runs-on: self-hosted
    needs: ['test-and-lint', 'validate-tag']
    permissions:
      contents: write
      packages: write
    steps:
      - name: Install Golang
        uses: actions/setup-go@v6
        with:
          go-version: ~1.23

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Github registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: refs/tags/${{ needs.validate-tag.outputs.tag }}
      
      - name: Run GoReleaser
        id: goreleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "2.9.0"
          args: --config .goreleaser.yaml --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  helm-chart:
    runs-on: self-hosted
    needs: ['validate-tag', 'build-and-publish', 'set-skip-app-release']
    if: ${{ needs.set-skip-app-release.outputs.skip-app-release == 'true' || needs.build-and-publish.result == 'success' }}
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refs/tags/${{ needs.validate-tag.outputs.tag }}

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: "4.0.1"

      - name: Log in to the Github registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io --username ${{ github.actor }} --password-stdin

      - name: Package chart
        run: |
          cd deploy
          helm package chart

      - name: Store chart name and version
        id: chart_data
        run: |
          cd deploy/chart
          chart_name=$(yq '.name' Chart.yaml)
          echo "chart_name=$chart_name" >> $GITHUB_OUTPUT

          chart_version=$(yq '.version' Chart.yaml)
          echo "chart_version=$chart_version" >> $GITHUB_OUTPUT

      - name: Push chart to registry
        run: |
          cd deploy
          helm push ${{ steps.chart_data.outputs.chart_name }}-${{ steps.chart_data.outputs.chart_version }}.tgz oci://ghcr.io/${{ github.repository_owner }}/fita/charts
      
      - name: Upload binaries to release
        run: |
          gh release view "$TAG" || (echo "Release not found"; exit 1)
          mv deploy/${{ steps.chart_data.outputs.chart_name }}-${{ steps.chart_data.outputs.chart_version }}.tgz deploy/${{ steps.chart_data.outputs.chart_name }}-chart-${{ steps.chart_data.outputs.chart_version }}.tgz
          gh release upload "$TAG" deploy/${{ steps.chart_data.outputs.chart_name }}-chart-${{ steps.chart_data.outputs.chart_version }}.tgz --clobber
        env:
          TAG: ${{ needs.validate-tag.outputs.tag }}
          GH_TOKEN: ${{ github.token }}
